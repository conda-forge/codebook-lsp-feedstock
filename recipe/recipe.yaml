context:
  version: "0.3.33"

package:
  name: codebook-lsp
  version: ${{ version }}

source:
  url: https://github.com/blopker/codebook/archive/refs/tags/v${{ version }}.zip
  sha256: fc3c1f747289b9ce369454b41b4c4fad344b8018f2bb5f780f7c92a5183662cd

build:
  number: 0
  script:
    env:
      CARGO_PROFILE_RELEASE_STRIP: symbols
      CARGO_PROFILE_RELEASE_LTO: fat
    content:
      - if: unix
        then: |
          export CFLAGS="${CFLAGS} -D_BSD_SOURCE"
          cargo-bundle-licenses --format yaml --output ${SRC_DIR}/THIRDPARTY.yml
          cargo auditable install --locked --no-track --path ./crates/codebook-lsp --root ${PREFIX}
        else: |
          cargo-bundle-licenses --format yaml --output %SRC_DIR%\THIRDPARTY.yml
          cargo auditable install --locked --no-track --path .\crates\codebook-lsp --root %LIBRARY_PREFIX%

requirements:
  build:
    - ${{ compiler('rust') }}
    - ${{ stdlib('c') }}
    - ${{ compiler('cxx') }}
    - cargo-bundle-licenses
    - cargo-auditable

tests:
  - script:
      - codebook-lsp --version

about:
  homepage: https://github.com/blopker/codebook
  license: MIT
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  description: "Spell Checker for Code"
  summary: "Spell Checker for Code"

extra:
  recipe-maintainers:
    - VasanthakumarV
